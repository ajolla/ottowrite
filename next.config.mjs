/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Configure if you later use next/image
    remotePatterns: [],
  },
};

export default nextConfig;
